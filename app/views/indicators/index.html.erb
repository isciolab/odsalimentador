
<div class="breadcrumbs">
  <div class="col-sm-4">
    <div class="page-header float-left">
      <div class="page-title">
        <h1>Indicadores#index</h1>
      </div>
    </div>
  </div>
  <div class="col-sm-8">
    <div class="page-header float-right">
      <div class="page-title">
        <ol class="breadcrumb text-right">
          <li><a href="#">Indicadores</a></li>
          <li class="active">Parametrizar Indicadores</li>
        </ol>
      </div>
    </div>
  </div>

  <div class="col-md-2">
    <div class="form-group">
      <label for="">Filtrar por ODS</label>
    </div>
  </div>
  <div class="col-md-2">
    <div class="form-group">

      <%= select("goal", "goal_id", Goal.all.collect {|p| [ p.number, p.id ] },
                 { include_blank: false,
                   prompt: "Seleccione Un ODS"},
                 {:class => "form-control chosen"}) %>

    </div>
  </div>

  <div class="col-md-2">
    <div class="form-group">
      <label for="">Filtrar por Meta</label>
    </div>
  </div>
  <div class="col-md-2">
    <div class="form-group">

      <%= select("target", "target_id", Target.all.collect {|p| [ p.number, p.id ] },
                 { include_blank: false,
                   prompt: "Seleccione Una Meta"},
                 {:class => "form-control chosen"}) %>

    </div>
  </div>

  <div class="col-sm-2">
    <div class="float-left">
      <%= button_to "Nuevo Indicador", { action: "new" },data:{target:"#modalindicador",toggle:"modal"}, remote: true, class: "btn btn-info",
                    :method => :get %>
    </div>
  </div>

</div>

<div class="content mt-3">

  <div class="animated fadeIn">
    <div class="row">

      <div class="col-md-12">
        <div class="card">
          <div class="card-header">
            <strong class="card-title">Lista de Indicadores</strong>
          </div>
          <div class="card-body">
            <table id="tblindicador" class="table table-striped table-bordered">
              <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Nombre</th>
                <th scope="col">Descripci&oacute;n</th>
                <th scope="col">Meta</th>
                <th scope="col">Disponible</th>
                <th scope="col">Acci&oacute;n</th>
              </tr>
              </thead>
              <tbody>


              <% @indicators.each do |row| %>
                <tr>
                  <th scope="row"><%= row.number %></th>
                  <td><%= row.name %></td>
                  <td><%= row.description %></td>
                  <td><%= row.target.number %></td>
                  <td>
                    <% if row.available==true
                         concat "SI";
                       else
                         concat  "NO";
                       end %>
                  </td>
                  <td><%= button_to "Editar", {action: "edit", id: row.id},data:{target:"#modalindicador",toggle:"modal"}, remote: true, class:"btn btn-primary",
                                    :method => :get %>
                    <button type="button" class="btn btn-danger">Eliminar</button></td>
                </tr>
              <% end %>

              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div><!-- .animated -->
</div>

<div class="modal fade" id="modalindicador" tabindex="-1" role="dialog"
     aria-labelledby="mediumModalLabel" aria-hidden="true">
</div>

<script type="text/javascript">
    $(document).ready(function() {
        $('#tblindicador').DataTable();
        $('#modalindicador').on('hidden.bs.modal', function (e) {
        });
    } );
</script>

