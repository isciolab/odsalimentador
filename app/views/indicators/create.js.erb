
<% if @indicator.errors.empty? %>

<% else %>
/*entra aqui solo cuando retorna error al guardar*/
Utilities.alertModal("<%= j @indicator.errors.full_messages.join('; ') %>", 'error', true);

<% end %>