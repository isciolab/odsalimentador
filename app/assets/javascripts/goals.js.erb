var Goal = {
    init: function () {

        $('#modalods').on('hidden.bs.modal', function (e) {
            $("#modalodsdialog").html("");
        });

        TablesDatatables.init();

    },
    initModal: function () {

        setTimeout( function (){
            $('*[autofocus]').focus()
        },500)

        $(document).on('ajax:success','#form',function(data){
            Utilities.alertModal("Los datos fueron guardados con éxito", 'error', true);
            $('#modalods').modal('hide')
        });

        $(document).on('ajax:error','#form',function(data){
            var mensaje
            jQuery.each( data.error, function( i, val ) {
                mensaje=""+i+" "+val
                Utilities.alertModal(message, 'error', true);
            });
        });

    }

}


