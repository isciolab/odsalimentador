
/*llamo el mismo form, pero le paso las variables como los titulos, ya que se usa la misma vista para edit*/
$("#modaluserdialog").html('<%= j render "form", {title: "Nuevo Departamento",title_button:"Crear"} %>');
$(function() {
    setTimeout( function (){
        $('*[autofocus]').focus()
    },500)
});