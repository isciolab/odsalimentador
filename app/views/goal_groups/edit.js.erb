
$(function() {
    $("#modalgrupoods").modal('show')
    setTimeout(function () {
        /*llamo el mismo form, pero le paso las variables como los titulos, ya que se usa la misma vista para edit*/
        $("#modalodsgroupdialog").html('<%= j render "form", {title: "Editar Grupo ODS",title_button:"Actualizar"} %>');
        $('*[autofocus]').focus()
    }, 500)

})
