
<% if @groupcity.errors.empty? %>

<% else %>
/*entra aqui solo cuando retorna error al guardar*/
Utilities.alertModal("<%= j @groupcity.errors.full_messages.join('; ') %>", 'error', true);

<% end %>