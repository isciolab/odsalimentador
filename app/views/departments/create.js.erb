<% if @department.errors.empty? %>

<% else %>
/*entra aqui solo cuando retorna error al guardar*/
Utilities.alertModal("<%= j @department.errors.full_messages.join('; ') %>", 'error', true);

<% end %>