<% if @goalgroup.errors.empty? %>

<% else %>
/*entra aqui solo cuando retorna error al guardar*/
Utilities.alertModal("<%= j @goalgroup.errors.full_messages.join('; ') %>", 'error', true);

<% end %>